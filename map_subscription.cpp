#include <iostream>
#include <map>

using namespace std;

int main()
{
    map<int, int> dict;

    if (dict[10] == 10);
    if (dict[10] == 10);
    if (dict[5] == 5);
    if (dict[15]);
    if (dict[25]);
    if (dict[32]);
    if (dict[9]);

    for (auto it = dict.begin(); it != dict.end(); ++it)
        cout << it->first << " = " << it->second << endl;

    return 0;
}

